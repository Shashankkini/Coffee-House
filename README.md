For the backend you need to install spring boot and connect to the database
to run it correctly and update the port of backend to the front-end angular.
You can run the angular part in Github itself.
